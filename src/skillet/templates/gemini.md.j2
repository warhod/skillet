# Skillet

This project uses Skillet for consistent agent guidance in the **Gemini** CLI and related workflows.

## Available skills

{{ skills_xml }}

## How to use

When a task matches a skill, read that skill's `SKILL.md` at the path in `<location>` before acting.

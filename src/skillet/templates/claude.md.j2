# Skillet

This project uses Skillet for consistent agent guidance in **Claude Code** and other tools.

## Available skills

{{ skills_xml }}

## How to use

When a task matches a skill, read that skill's `SKILL.md` at the path in `<location>` before acting. Claude Code also discovers skills under `.claude/skills/<name>/SKILL.md`.
